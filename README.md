#include <stdio.h>
int main(void) {
  int n;
printf("\n enter the number");
  scanf("%d",&n);
  if(n%2==0)
    printf("\n The give number is even");
  else
    printf("\n the given number is odd");
	return 0;
}
