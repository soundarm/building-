#include <stdio.h>
int main()
{
  int a,b,c;
printf("\n enter the number");
  scanf("%d%d%d",&a,&b,&c);
  if(a>b&&a>c)
  printf(" %dis greate",a);
   {
       if(b>a&&b>c)
       printf(" %d is grester",b);
 else
    printf(" %d is greater",c);
   }
	return 0;
}
